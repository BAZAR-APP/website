import { Button } from '@/components'
import React from 'react'

const page = () => {
  return (
    <div>
      <Button intent="transperent" size="md" onClick={() => {}}>
        Click Now
      </Button>
    </div>
  )
}

export default page
